import { Hoverlineanimation } from './hoverlineanimation';

describe('Hoverlineanimation', () => {
  it('should create an instance', () => {
    const directive = new Hoverlineanimation();
    expect(directive).toBeTruthy();
  });
});
